package com.hardware.erp.product.substitute.entity;

/** CR-089. OPEN until the owner either offered an alternative (RESOLVED) or gave up on the request (CANCELLED). */
public enum ProductRequestStatus {
    OPEN, RESOLVED, CANCELLED
}
