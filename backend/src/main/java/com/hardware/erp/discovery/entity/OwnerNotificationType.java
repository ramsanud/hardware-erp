package com.hardware.erp.discovery.entity;

/** CR-090 (first writer) - the in-app notification kinds. CR-092 adds DAILY_SUMMARY/LOW_STOCK writers. */
public enum OwnerNotificationType {
    PRODUCT_DISCOVERY, DAILY_SUMMARY, LOW_STOCK, SYSTEM
}
