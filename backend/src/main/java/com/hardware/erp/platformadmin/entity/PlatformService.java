package com.hardware.erp.platformadmin.entity;

/** The services System Health checks and Incidents can be filed against. */
public enum PlatformService {
    BACKEND,
    DATABASE,
    AUTHENTICATION,
    STORAGE,
    WHATSAPP,
    EMAIL,
    BACKGROUND_JOBS
}
